
package common

templ NavBar(title string, items ...templ.Component) {
  <nav hx-boost="true">
    <ul>
      <li>@Link("/", title, nil)</li>
    </ul>
    <ul>
      for _, item := range items {
        <li>@item</li>
      }
    </ul>
  </nav>
}
